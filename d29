491.递增子序列 
不能排序，所以无法用used[i]==used[i-1]这样去重。
陷阱在于，需要判断同一父节点下重复使用过与否。于是用count/find

46. 全排列
used数组，其实就是记录此时path里都有哪些元素使用了，一个排列里一个元素只能使用一次。
排列问题的不同：
每层都是从0开始搜索而不是startIndex
需要used数组记录path里都放了哪些元素了

47.全排列 II 
重点在于去重逻辑
同一层要去重
